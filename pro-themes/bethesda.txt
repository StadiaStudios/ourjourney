/* Christmas Wonderland Theme
   Light Mode Only
*/

:root {
    --xmas-red: #c41e3a;
    --xmas-gold: #d4af37;
    --xmas-green: #165b33;
    --xmas-snow: #f8f9fa;
    --xmas-text: #2d3436;
}

body {
    background-color: #d8d8d8 !important;
    background-image: radial-gradient(#d6d6d6 2px, transparent 2px), radial-gradient(#cecaca 2px, transparent 2px) !important;
    background-size: 50px 50px !important;
    background-position: 0 0, 25px 25px !important;
    color: var(--xmas-text) !important;
    position: relative;
    overflow-x: hidden;
}

/* Snowflake Animation */
body::before {
    content: "";
    position: fixed;
    top: -100px;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 9999;
    pointer-events: none;
    background-image: 
        radial-gradient(4px 4px at 20px 30px, #ffffff, rgba(0,0,0,0)),
        radial-gradient(6px 6px at 40px 70px, #ffffff, rgba(0,0,0,0)),
        radial-gradient(5px 5px at 50px 160px, #ffffff, rgba(0,0,0,0)),
        radial-gradient(4px 4px at 90px 40px, #ffffff, rgba(0,0,0,0)),
        radial-gradient(6px 6px at 110px 110px, #ffffff, rgba(0,0,0,0));
    background-size: 200px 200px;
    animation: snowFall 10s linear infinite;
}

@keyframes snowFall {
    0% { transform: translateY(0); }
    100% { transform: translateY(100vh); }
}

/* Header Adjustments */
header {
    background-color: rgba(196, 30, 58, 0.9) !important; /* Festive Red */
    border-bottom: 3px solid var(--xmas-gold) !important;
    backdrop-filter: blur(8px);
}

header h1 {
    color: white !important;
    text-shadow: 1px 1px 2px rgba(0,0,0,0.2);
}

header svg {
    color: white !important;
}

/* Container Adjustments */
#app, .bg-white, .bg-pink-50 {
    background-color: rgba(255, 255, 255, 0.9) !important;
    border: 2px solid var(--xmas-red) !important;
    border-radius: 20px !important;
    box-shadow: 0 8px 32px rgba(196, 30, 58, 0.15) !important;
}

/* Typography & Colors */
.text-pink-700, .text-pink-600 {
    color: var(--xmas-red) !important;
}

.duration-value {
    color: var(--xmas-green) !important;
    font-weight: 900 !important;
}

#durationList li::before {
    content: '❄️' !important;
}

/* Buttons */
button, #setDateButton, #saveNames {
    background-color: var(--xmas-red) !important;
    border: 2px solid var(--xmas-gold) !important;
    color: white !important;
    border-radius: 50px !important;
    font-family: 'Inter', sans-serif;
    letter-spacing: 0.5px;
}

button:hover {
    background-color: var(--xmas-green) !important;
    transform: scale(1.02);
}

/* Inputs */
input[type="date"], input[type="text"] {
    border: 2px solid #e2e8f0 !important;
    background: white !important;
    color: #333 !important;
}

input:focus {
    border-color: var(--xmas-red) !important;
    box-shadow: 0 0 0 3px rgba(196, 30, 58, 0.1) !important;
}

/* Anniversary Cards */
.milestone-card {
    background: white !important;
    border-left: 5px solid var(--xmas-green) !important;
}

/* Photo Wrapper */
#photoContainer {
    border: 5px solid white !important;
    box-shadow: 0 4px 15px rgba(0,0,0,0.1) !important;
    outline: 2px solid var(--xmas-gold) !important;
}

/* Scrollbar */
::-webkit-scrollbar-thumb {
    background: var(--xmas-red) !important;
}

::-webkit-scrollbar-track {
    background: var(--xmas-snow) !important;
}

/* Hide Dark Mode Toggle specifically for this theme since it's light-only */
#darkModeToggle, label[for="darkModeToggle"], .dark-mode-section {
    display: none !important;
}